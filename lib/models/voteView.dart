class VoteView {
  String ? date;
  String ? money;
  String ? punish;
  String ? daDauGia;
  String ? ky;
  String ? chu;
  String ? thamGia;
  bool ? isLate;

  VoteView({
      this.date,
      this.money,
      this.punish,
      this.daDauGia,
      this.ky,
      this.chu,
      this.thamGia,
      this.isLate
  });
}