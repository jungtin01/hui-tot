import 'package:flutter/material.dart';

class WalletHistory {
  String ? name;
  String ? money;
  String ? date;
  bool ? isGetMony;

  WalletHistory({this.name, this.money, this.date, this.isGetMony});
}