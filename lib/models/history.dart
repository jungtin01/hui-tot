import 'package:flutter/material.dart';

class History {
  String ? ngayGop;
  String ? nguoiGop;
  String ? soTienGop;
  int ? checkState;

  History({this.ngayGop, this.nguoiGop, this.soTienGop, this.checkState});
}